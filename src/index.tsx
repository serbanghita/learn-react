import * as React from "react";
import * as ReactDOM from "react-dom";
import App from "./component/App";

ReactDOM.render(<App title="React and TypeScript demo!" titleColor="blue"/>, document.getElementById("app"));
